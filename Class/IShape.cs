namespace BasicsOOP.Class
{
    internal interface IShape
    {
        // Interface methods, contract for the classes that implements the interface
        double GetArea();

        double GetPerimeter();

        double GetVolume();
    }
}